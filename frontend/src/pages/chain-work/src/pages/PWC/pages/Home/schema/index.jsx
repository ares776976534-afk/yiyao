import column from './column';
import filter from './filter';
import batchAction from './batchAction';

export default {
  colSchema: column,
  batchActionSchema: batchAction,
  // messageSchema: messageSchema_Replenishment,
  filterSchema: filter,
};
