---
description: CSS/SCSS 书写顺序规范
globs: "**/*.scss,**/*.css,**/*.module.scss"
alwaysApply: true
---

## CSS 属性书写顺序规范

按照以下分组顺序书写 CSS 属性，组与组之间不需要空行，保持紧凑。

### 0. CSS 自定义属性（Custom Properties）
局部变量声明，写在所有属性的最前面。

```
--变量名
```

### 1. 定位（Positioning）
控制元素的定位方式和坐标，优先级最高，影响文档流。

```
position
inset
inset-block-start / inset-inline-start / inset-inline-end
top / right / bottom / left
z-index
```

### 2. 盒模型（Box Model）
决定元素的尺寸与空间占用。

```
display
/* Flex */
flex / flex-grow / flex-shrink / flex-direction / flex-flow / flex-wrap
justify-content / justify-self
align-content / align-items / align-self
gap / column-gap / row-gap
/* Grid */
grid-template-columns / grid-template-rows
grid-row-start / grid-row-end
/* Float */
float / clear
/* 尺寸 */
box-sizing
container-type
aspect-ratio
width / min-width / max-width
height / min-height / max-height
/* 间距 */
margin / margin-top / margin-right / margin-bottom / margin-left
margin-block / margin-inline / margin-inline-end
padding / padding-top / padding-right / padding-bottom / padding-left
padding-block / padding-inline / padding-inline-start / padding-inline-end
/* 边框 */
border / border-top / border-right / border-bottom / border-left
border-width / border-style / border-color / border-radius
border-collapse / border-spacing
outline
/* 溢出与滚动 */
overflow / overflow-x / overflow-y
overscroll-behavior-x
scrollbar-width / scrollbar-color
```

### 3. 排版（Typography）
文字相关的样式属性。

```
font / font-family / font-size / font-weight / font-style / font-variant
line-height
letter-spacing / word-spacing / word-break / word-wrap
text-align / text-indent / text-decoration / text-transform / text-overflow
white-space
vertical-align
list-style / list-style-type
/* 多行省略（无标准替代，仍需 -webkit- 前缀，各主流浏览器均已跟进支持） */
display: -webkit-box
-webkit-box-orient: vertical
-webkit-line-clamp
overflow: hidden
```

### 4. 视觉（Visual）
颜色、背景、阴影、滤镜等视觉呈现。

```
color
background / background-color / background-image / background-repeat / background-position / background-size
background-clip
opacity
visibility
isolation
mix-blend-mode
box-shadow
text-shadow
filter
backdrop-filter
mask / mask-composite
object-fit
zoom
```

### 5. 动效（Animation）
过渡与动画。

```
transition
animation / animation-delay / animation-fill-mode
transform / transform-origin
will-change
```

### 6. 表格（Table）
表格布局相关属性。

```
table-layout
caption-side
```

### 7. 其他（Misc）
光标、指针事件、用户选择、计数器等。

```
cursor
pointer-events
user-select
resize
content
counter-reset / counter-increment
```

---

## 示例

```scss
.card {
  // 0. CSS 自定义属性
  --card-bg: #fff;
  // 1. 定位
  position: absolute;
  top: 0;
  left: 0;
  z-index: 10;
  // 2. 盒模型
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  box-sizing: border-box;
  aspect-ratio: 16 / 9;
  width: 320px;
  height: 200px;
  margin: 0 auto;
  padding: 16px 24px;
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  overflow: hidden;
  scrollbar-width: none;
  // 3. 排版
  font-size: 14px;
  font-weight: 500;
  line-height: 1.5;
  text-overflow: ellipsis;
  white-space: nowrap;
  // 4. 视觉
  color: #1f2937;
  background-color: var(--card-bg);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  filter: blur(0);
  backdrop-filter: blur(8px);
  // 5. 动效
  transition: box-shadow 0.2s ease;
  will-change: transform;
  // 7. 其他
  cursor: pointer;
  pointer-events: auto;
}
```

---

## 补充约定

* SCSS 嵌套时，先写当前选择器的属性，再写伪类（`&:hover`、`&:focus`），最后写子选择器嵌套
* 媒体查询（`@media`）统一写在对应选择器块的末尾
* CSS 自定义属性（`--var`）声明写在属性列表的最前面（第 0 组）
* 禁止同一属性在同一选择器中重复声明
* `inset` 是 `top/right/bottom/left` 的逻辑属性简写，优先使用 `inset` 减少代码量
* 0px可以简写0，#FFFFFF可以简写#FFF
* 布局要自适应，根据设计稿元素之间的间距推算出哪些是固定宽高，哪些是可以自适应的
* 根据文本的长度和下方留白距离推算是需要换行还是省略，或者是多行省略
* 根样式中会统一声明一些样式，不用给每个选择器都写一遍，比如box-sizing
* 有些很高频的浏览器默认样式不需要主动去写，比如：flex-direction: row
