---
description: 
globs: 
alwaysApply: true
---
* 用中文交流
* 阅读知识库、文档、图片、视频分析需求的过程中，碰到无法访问的情况要及时汇报，并禁止自行猜测需求生成代码
* 遇到描述不清的内容，可以停下来和我对话问清楚需求
* 主动纠正术语错误、语法错误、变量命名错误
* 如果我对某个技术理解错了，要主动科普消除认知偏差
* 开发独立模块时优先把所有的代码放在一个目录下，不要污染公共目录和文件。等我确认好生成的代码是合格的，再合并到公共代码中
* 禁止把自行思考出来的需求和功能写入工程代码中，你可以把想法输出到临时文件中，我觉得有用再采纳
* 禁止修改我提供给你查看的文档
* cursor在思考和生成代码的过程中会自动创建很多文档文件，一个组件的文档全部都写在一个文件中即可。内容需要cursor自己能看懂并知道上下文，便于在任何时候任何会话中，你都能根据这个文档恢复过往的记忆
* cursor生成的测试文件全部集中放根目录test目录下，example目录也是一样，不要创建得到处都是
* 在聊天中给了你多个上下文，优先读图片，然后读链接，最后读文件和目录
* 一些常用的功能组件，优先从依赖库中合适的，然后在开源社区中找，不要自己把基础组件实现一遍
* 禁止硬编码写逻辑，尤其是宽、高、坐标、价格、数字等，要谨慎确认
* 给你输入文件路径，你在生成代码之前优先读取文件同级的.mdc文件并按照其规范开发。如果同级没有，一直向上找。如果找到了根目录，读取根目录和.cursor目录下的.mdc并遵循规范
* 给你设计稿链接或者是别的参考网页链接，如果无法读取，直接告警并停止任务，不要凭自己的感觉生成代码



## 设计稿读取

设计稿链接使用MCP读取，读取不出来的时候不要继续执行任务，要停下来告知

设计稿相关的链接类似这种形式 https://pixelator.alibaba-inc.com/page/ast/1775645262951-186762843000654-219%3A5534-219%3A11255

读取设计稿的MCP大致如下面配置所示，以pixelator为关键字

```
"pixelator": {
  "command": "pixelator-mcp",
  "args": [
    "@ali/pixelator-cli@latest"
  ]
}

```

有时候会取另一个别名

```
"1d": {
  "command": "pixelator-mcp",
  "args": [
    "@ali/pixelator-cli@latest"
  ]
}
```

## 网页内容读取

```
"playwright": {
  "command": "npx",
  "args": [
    "@playwright/mcp@latest"
  ],
  "disabled": false
},
```
